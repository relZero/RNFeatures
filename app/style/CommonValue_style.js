import DensityUtils from '../utils/density_utils';

const CommonValue = {
    bodyBackground: '#f5f5f5',
    lightColor: '#fff',
    deepColor: '#333',
    fontSizeS: DensityUtils.scaleSize(24),
    fontSizeM: DensityUtils.scaleSize(28),
    fontSizeL: DensityUtils.scaleSize(36),
    fontSizeX: DensityUtils.scaleSize(48),
};

export default CommonValue;